# Contributing

Thanks for helping improve timewarp-ci.

## Development

```sh
npm install
npm test
npm run typecheck
npm run build
```

Keep changes small, deterministic, and covered by tests when behavior changes.

## Scope

timewarp-ci starts with JavaScript, TypeScript, and Node projects. Avoid adding
heavy dependencies or broad platform claims unless the behavior is implemented
and tested.
